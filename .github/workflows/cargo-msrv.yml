name: "Run cargo-msrv"
on:
  workflow_dispatch:
  push:
    branches: ["**"]
    paths: ["**/Cargo.toml", "**/Cargo.lock"]

jobs:
  cargo-msrv:
    permissions:
      contents: write
      pull-requests: write
    uses: iiiii7d/cargo-msrv-workflow/.github/workflows/workflow.yml@4ca8f8d27785e6f7b2b1cb956f08b87f57a397f9 # main
    with:
      working-directory: gatelogue-types-rs
