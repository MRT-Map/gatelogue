name: "Run cargo-msrv"
on:
  workflow_dispatch:
  push:
    branches: ["**"]
    paths: ["**/Cargo.toml", "**/Cargo.lock"]

jobs:
  cargo-msrv:
    permissions:
      contents: write
      pull-requests: write
    uses: iiiii7d/cargo-msrv-workflow/.github/workflows/workflow.yml@152bb7132a819472903318cba5f0bb00bb25dac0 # main
    with:
      working-directory: gatelogue-types-rs
